# both make a hash with single key value pair
name: 'Cole'
occupation: 'Software Developer'

# makes hash within hash, parent hash is "hobbies" with contains hashses "Running and "Drifting"
hobbies:
  Running:
    level: 'Above Average'
    time: '4 months'
  Drifting:
    level: 'Intermediate'
    time: '3 years'

# makes hash called traits with its value as an array with values "noob" and "gibby"
traits:
  - noob
  - gibby